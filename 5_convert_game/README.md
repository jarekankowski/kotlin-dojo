The task is to convert the Game class from Java to Kotlin.

Be aware that in Kotlin all exception are unchecked.

In Kotlin you can have a default value assigned to a parameter at the declaration site.